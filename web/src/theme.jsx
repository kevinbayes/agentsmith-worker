import {createTheme} from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: 'dark',
  },
  typography: {
    h1: {
      fontSize: "2.3em",
    },
    h2: {
      fontSize: "2.1em",
    },
    h3: {
      fontSize: "1.9em",
    },
    h4: {
      fontSize: "1.7em",
    },
    h5: {
      fontSize: "1.5em",
    }
  }
});